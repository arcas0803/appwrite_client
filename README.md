# AppwriteClient

## Getting Started

### Installation

Add the following to your `pubspec.yaml` file:

```yaml

dependencies:
  appwrite_client: 
    git:
      url: https://github.com/arcas0803/appwrite_client.git
      ref: main

```
